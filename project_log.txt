GadgetGrid - Project Execution Log
===================================
এই ফাইলে প্রতিটি কমপ্লিট হওয়া স্টেপের বিস্তারিত কাজ এবং টেকনোলজির বিবরণ লেখা হবে।

Step 1: Initialize Next.js project with TypeScript & Initialize Git Repository
-----------------------------------------------------------------------------
- Technologies Used: Next.js 15 (App Router), TypeScript, Git.
- How it was done: 
  * `create-next-app` কমান্ড ব্যবহার করে প্রজেক্ট তৈরি করা হয়েছে।
  * TypeScript, Tailwind CSS, ESLint, এবং App Router এনাবল করা হয়েছে।
  * গিট ইনিশিয়ালাইজ করে আপনার দেওয়া গিটহাব রিপোজিটরির (`sanoyon211/GadgetGrid`) সাথে লোকাল প্রজেক্ট কানেক্ট করা হয়েছে।
  * প্রথম কমিট করে `main` ব্রাঞ্চে পুশ করা হয়েছে।

Step 2: Configure Tailwind CSS, custom fonts (Outfit & Inter), and base CSS
-----------------------------------------------------------------------------
- Technologies Used: Tailwind CSS v4, Google Fonts (next/font/google).
- How it was done:
  * `src/app/layout.tsx` ফাইলটি মডিফাই করে `Outfit` (হেডিংয়ের জন্য) এবং `Inter` (সাধারণ লেখার জন্য) ফন্ট ইমপোর্ট করে যুক্ত করা হয়েছে।
  * `src/app/globals.css` ফাইলে Tailwind v4 এর `@theme` রুল ব্যবহার করে "Sunset Minimalist" থিমের কালার কোডগুলো গ্লোবাল ভ্যারিয়েবল হিসেবে অ্যাড করা হয়েছে (যাতে পুরো প্রজেক্টে সহজেই ব্যবহার করা যায়)।
  * কোডগুলো গিটহাবে পুশ করা হয়েছে।

Step 3 & 4: Setup UI dependencies & Design System
-----------------------------------------------------------------------------
- Technologies Used: npm, lucide-react, sonner, recharts.
- How it was done:
  * আইকন দেখানোর জন্য `lucide-react` ইনস্টল করা হয়েছে।
  * সুন্দর টোস্ট মেসেজের জন্য `sonner` ইনস্টল করা হয়েছে।
  * ড্যাশবোর্ডে চার্ট/গ্রাফ দেখানোর জন্য `recharts` ইনস্টল করা হয়েছে।
  * (নোট: Step 4 এর ডিজাইন সিস্টেম কনফিগারেশনের কাজ আমরা আগেই Step 2-এ Tailwind v4 এর মাধ্যমে `globals.css`-এ কমপ্লিট করে ফেলেছি)।
  * গিটহাবে পুশ করা হয়েছে।

Step 5: Build Global Components (Navbar & Footer)
-----------------------------------------------------------------------------
- Technologies Used: Next.js (App Router), Tailwind CSS, lucide-react.
- How it was done:
  * `src/components/globals/Navbar.tsx` তৈরি করে রেসপন্সিভ (মোবাইল হ্যামবার্গার মেনু) এবং স্টিকি নেভিগেশন বার ডিজাইন করা হয়েছে।
  * `src/components/globals/Footer.tsx` তৈরি করে ৪-কলাম বিশিষ্ট একটি সুন্দর ফুটার ডিজাইন করা হয়েছে।
  * `src/app/layout.tsx` ফাইলে Navbar এবং Footer ইমপোর্ট করে যুক্ত করা হয়েছে, যাতে এগুলো ওয়েবসাইটের প্রতিটি পেজে দেখায়।
  * গিটহাবে পুশ করা হয়েছে।

Step 6: Build Homepage Sections with Separate Components
-----------------------------------------------------------------------------
- Technologies Used: Next.js, Tailwind CSS, lucide-react.
- How it was done:
  * `src/components/home/HeroSection.tsx` তৈরি করে একটি আকর্ষণীয় হিরো ব্যানার ডিজাইন করা হয়েছে।
  * `src/components/home/FeaturedCategories.tsx` তৈরি করে বিভিন্ন গ্যাজেট ক্যাটাগরির গ্রিড তৈরি করা হয়েছে।
  * `src/components/home/TrendingGadgets.tsx` তৈরি করে ট্রেন্ডিং প্রোডাক্টের লিস্ট ডিজাইন করা হয়েছে।
  * `src/components/home/NewsletterSection.tsx` তৈরি করে ইমেইল সাবস্ক্রিপশনের অংশ তৈরি করা হয়েছে।
  * `src/app/page.tsx` থেকে ডিফল্ট Next.js কোড সরিয়ে এই সবগুলো কম্পোনেন্ট একসাথে যুক্ত করা হয়েছে।

Step 7: Build Explore/Shop Page
-----------------------------------------------------------------------------
- Technologies Used: Next.js (App Router), Tailwind CSS, React State.
- How it was done:
  * `src/components/shop/ProductFilters.tsx` তৈরি করে ক্যাটাগরি, দাম এবং রেটিং ফিল্টার করার জন্য একটি সাইডবার ফিল্টার মেনু ডিজাইন করা হয়েছে।
  * `src/components/shop/SearchAndSort.tsx` তৈরি করে একটি সার্চবার এবং প্রোডাক্ট সর্ট (Sort) করার ড্রপডাউন মেনু যুক্ত করা হয়েছে।
  * `src/components/shop/ProductGrid.tsx` তৈরি করে ডামি প্রোডাক্ট দিয়ে একটি সুন্দর ও রেস্পন্সিভ প্রোডাক্ট গ্রিড বানানো হয়েছে।
  * `src/app/products/page.tsx` এ একটি নতুন রাউট তৈরি করে উপরের সবগুলো কম্পোনেন্ট লেফট সাইডবার লেআউটে সাজানো হয়েছে।

Step 8: Build Product Details Page (Dynamic Route)
-----------------------------------------------------------------------------
- Technologies Used: Next.js Dynamic Routes (`[id]`), Tailwind CSS, lucide-react.
- How it was done:
  * `src/components/product/ProductGallery.tsx` তৈরি করে প্রোডাক্টের ছবি ও থাম্বনেইল গ্যালারি বানানো হয়েছে।
  * `src/components/product/ProductInfo.tsx` তৈরি করে প্রোডাক্টের নাম, দাম, রেটিং, ডেসক্রিপশন এবং কালার/স্টোরেজ সিলেক্ট করার অপশন যুক্ত করা হয়েছে।
  * `src/components/product/ProductActions.tsx` তৈরি করে Add to Cart, Buy Now, এবং Wishlist বাটনগুলো তৈরি করা হয়েছে।
  * `src/components/product/ProductTabs.tsx` তৈরি করে Description, Specifications, এবং Reviews দেখার জন্য একটি ডাইনামিক ট্যাব সিস্টেম বানানো হয়েছে।
  * `src/app/product/[id]/page.tsx` ডাইনামিক রাউট তৈরি করে সব কম্পোনেন্ট একসাথে যুক্ত করে প্রোডাক্ট ডিটেইলস পেজটি কমপ্লিট করা হয়েছে এবং নিচে Related Products হিসেবে TrendingGadgets অ্যাড করা হয়েছে।

Step 9: Build Cart & Checkout Page
-----------------------------------------------------------------------------
- Technologies Used: Next.js (App Router), Tailwind CSS, React State, lucide-react.
- How it was done:
  * `src/components/cart/CartItemsList.tsx` তৈরি করে কার্টে থাকা প্রোডাক্টগুলোর লিস্ট দেখানো হয়েছে। এখানে প্রোডাক্টের কোয়ান্টিটি বাড়ানো-কমানো বা ডিলিট করার ফাংশনালিটি (React State দিয়ে) যুক্ত করা হয়েছে। কার্ট খালি থাকলে একটি সুন্দর Empty State ও দেখাবে।
  * `src/components/cart/CartSummary.tsx` তৈরি করে সাব-টোটাল, ট্যাক্স এবং টোটাল প্রাইস হিসাব করার সামারি বক্স বানানো হয়েছে, সাথে প্রোমো কোড অ্যাপ্লাই করার ফিল্ডও আছে।
  * `src/app/cart/page.tsx` এ একটি নতুন রাউট তৈরি করে লেফট সাইডে Cart Items এবং রাইট সাইডে Cart Summary দিয়ে একটি রেস্পন্সিভ লেআউট তৈরি করা হয়েছে।

Step 10: Build Authentication Pages (Login & Register)
-----------------------------------------------------------------------------
- Technologies Used: Next.js (App Router), Tailwind CSS, React State, react-icons/fa.
- How it was done:
  * `src/components/auth/AuthLayout.tsx` তৈরি করে লগিন ও রেজিস্ট্রেশনের জন্য স্প্লিট-স্ক্রিন লেআউট তৈরি করা হয়েছে (একপাশে ফর্ম, অন্যপাশে সুন্দর ব্যানার ডিজাইন)।
  * `src/components/auth/LoginForm.tsx` তৈরি করে ইমেইল, পাসওয়ার্ড এবং সোশ্যাল লগিন (Google/GitHub) বাটনসহ একটি সুন্দর লগিন ফর্ম ডিজাইন করা হয়েছে।
  * `src/components/auth/RegisterForm.tsx` তৈরি করে নতুন ইউজারদের জন্য সাইন-আপ ফর্ম বানানো হয়েছে।
  * `src/app/login/page.tsx` এবং `src/app/register/page.tsx` রাউট তৈরি করে এই কম্পোনেন্টগুলো বসানো হয়েছে।

Step 11: Build User Dashboard
-----------------------------------------------------------------------------
- Technologies Used: Next.js (Nested Layouts), Tailwind CSS, lucide-react.
- How it was done:
  * `src/components/dashboard/DashboardSidebar.tsx` তৈরি করে ড্যাশবোর্ডের মেনু (Overview, Orders, Wishlist, Settings) সহ লেফট সাইডবার বানানো হয়েছে।
  * `src/components/dashboard/DashboardOverview.tsx` তৈরি করে ইউজারের অ্যাকাউন্ট সামারি এবং রিসেন্ট অর্ডার স্ট্যাটাস দেখানোর মেইন স্ক্রিন বানানো হয়েছে।
  * `src/components/dashboard/OrderHistory.tsx` তৈরি করে একটি সুন্দর টেবিল দিয়ে ইউজারের পূর্বের অর্ডারগুলো (Dummy Data) প্রদর্শন করা হয়েছে।
  * `src/app/dashboard/layout.tsx` তৈরি করে ড্যাশবোর্ডের জন্য একটি নেস্টেড লেআউট বানানো হয়েছে যেন সাইডবারটি সব পেজে থাকে।
  * `src/app/dashboard/page.tsx` এবং `src/app/dashboard/orders/page.tsx` রাউটগুলো তৈরি করা হয়েছে।

Step 12: Build Information Pages (About Us & Contact Us)
-----------------------------------------------------------------------------
- Technologies Used: Next.js (App Router), Tailwind CSS, lucide-react.
- How it was done:
  * `src/app/about/page.tsx` তৈরি করে ওয়েবসাইটের একটি সুন্দর About Us পেজ বানানো হয়েছে, যেখানে হিরো ব্যানার, Our Story, Core Values এবং Team মেম্বারদের সেকশন রয়েছে।
  * `src/app/contact/page.tsx` তৈরি করে কন্টাক্ট পেজ বানানো হয়েছে, যেখানে অফিসের ঠিকানা, ইমেইল, ফোন নম্বরের ইনফো কার্ড এবং একটি সুন্দর কন্টাক্ট ফর্ম যুক্ত করা হয়েছে।

Step 13: Build Checkout Flow (Shipping & Payment)
-----------------------------------------------------------------------------
- Technologies Used: Next.js (App Router), Tailwind CSS, React State, lucide-react.
- How it was done:
  * `src/components/checkout/CheckoutForm.tsx` তৈরি করে ইউজারের শিপিং অ্যাড্রেস (Name, Email, Address, ZIP, Phone) নেওয়ার জন্য ফর্ম ডিজাইন করা হয়েছে।
  * `src/components/checkout/PaymentMethods.tsx` তৈরি করে পেমেন্ট মেথড (Credit Card & Cash on Delivery) সিলেক্ট করার অপশন এবং ডামি কার্ড ডিটেইলস ফর্ম যুক্ত করা হয়েছে।
  * `src/components/checkout/OrderSummarySide.tsx` তৈরি করে চেকআউট পেজের ডানপাশে কার্ট আইটেম ও প্রাইস ক্যালকুলেশন দেখানো হয়েছে।
  * `src/app/checkout/page.tsx` রাউট তৈরি করে এই সবগুলো কম্পোনেন্ট দিয়ে একটি পূর্ণাঙ্গ চেকআউট পেজ লেআউট কমপ্লিট করা হয়েছে।

Step 14: Navigation Fixes & Final Polishing
-----------------------------------------------------------------------------
- Technologies Used: Next.js (App Router), Tailwind CSS.
- How it was done:
  * `src/components/globals/Navbar.tsx` এর লিংকগুলো আপডেট করে আসল পেজগুলোতে (Home, Shop, About, Contact) পয়েন্ট করা হয়েছে এবং Cart আইকনে লিংক এড করা হয়েছে।
  * `src/components/globals/Footer.tsx` এর কুইক লিংকগুলো আপডেট করা হয়েছে যেন ওয়েবসাইটের কোনো ব্রোকেন লিংক বা ডেড লিংক না থাকে।

Step 15: Interactive Navbar Features (Theme Toggle & Search Modal)
-----------------------------------------------------------------------------
- Technologies Used: Next.js (App Router), Tailwind CSS, next-themes.
- How it was done:
  * `next-themes` লাইব্রেরি ইনস্টল করে `src/components/globals/ThemeProvider.tsx` তৈরি করা হয়েছে।
  * `src/app/layout.tsx` এ ThemeProvider র‍্যাপ (wrap) করা হয়েছে যেন ম্যানুয়াল ডার্ক মোড সাপোর্ট করে।
  * `src/components/globals/SearchModal.tsx` তৈরি করা হয়েছে যা একটি সুন্দর সার্চ পপআপ দেখায়।
  * `Navbar.tsx` এ Sun/Moon আইকন দিয়ে ডার্ক মোড টগল এবং Search আইকনে ক্লিক করলে সার্চ মডাল ওপেন হওয়ার ফিচার যুক্ত করা হয়েছে।

Step 16: Setup MongoDB & Mongoose Database
-----------------------------------------------------------------------------
- Technologies Used: MongoDB, Mongoose, Node.js.
- How it was done:
  * `mongoose` প্যাকেজটি ইন্সটল করে `src/lib/mongoose.ts` এ ডেটাবেস কানেকশন তৈরি করা হয়েছে।
  * `src/models/User.ts` এবং `src/models/Gadget.ts` তৈরি করে ইউজার এবং প্রোডাক্টের স্কিমা (Schema) ডিফাইন করা হয়েছে।

Step 17: Configure NextAuth for Authentication
-----------------------------------------------------------------------------
- Technologies Used: NextAuth.js (v4), bcryptjs.
- How it was done:
  * `next-auth` এবং `bcryptjs` ইন্সটল করে `src/lib/auth.ts` এবং `src/app/api/auth/[...nextauth]/route.ts` তৈরি করা হয়েছে।
  * `src/components/globals/AuthProvider.tsx` তৈরি করে `layout.tsx` এ র‍্যাপ করা হয়েছে।
  * `src/app/api/auth/register/route.ts` এ কাস্টম রেজিস্ট্রেশন এপিআই তৈরি করে `LoginForm` এবং `RegisterForm` এর সাথে ব্যাকএন্ড কানেক্ট করা হয়েছে।

Step 18: Build Explore Page API & Integrate Filters
-----------------------------------------------------------------------------
- Technologies Used: Next.js API Routes, Server Components, URLSearchParams.
- How it was done:
  * `src/app/api/gadgets/route.ts` তৈরি করে ডেটাবেস থেকে প্রোডাক্ট ফেচ, ফিল্টার এবং সর্ট করার এপিআই তৈরি করা হয়েছে।
  * `src/app/api/seed/route.ts` তৈরি করা হয়েছে ডামি ডেটা দিয়ে ডেটাবেস পপুলেট করার জন্য।
  * `src/app/products/page.tsx` কে সার্ভার কম্পোনেন্ট বানিয়ে URL Search Params অনুযায়ী ডেটাবেস থেকে ডেটা এনে `ProductGrid`-এ পাস করা হয়েছে।
  * `ProductFilters.tsx` এবং `SearchAndSort.tsx` এ `useRouter` ব্যবহার করে ফিল্টারগুলো URL এ পুশ করার ব্যবস্থা করা হয়েছে।

Step 19: Dashboard Product Management & Protected Routes
-----------------------------------------------------------------------------
- Technologies Used: NextAuth Middleware, React State, Next.js API Routes.
- How it was done:
  * `src/middleware.ts` তৈরি করে `/dashboard` রাউটগুলোকে প্রটেক্ট করা হয়েছে (লগিন ছাড়া এক্সেস করা যাবে না)।
  * `src/app/dashboard/add-product/page.tsx` তৈরি করে নতুন প্রোডাক্ট অ্যাড করার ফর্ম বানানো হয়েছে।
  * `src/app/dashboard/products/page.tsx` তৈরি করে প্রোডাক্ট ম্যানেজ (ডিলিট) করার টেবিল বানানো হয়েছে।
  * `src/app/api/gadgets/add/route.ts` এবং `src/app/api/gadgets/[id]/route.ts` তৈরি করে Create এবং Delete এপিআই যুক্ত করা হয়েছে।
  * `DashboardSidebar.tsx` এ নতুন পেজগুলোর লিংক যুক্ত করা হয়েছে।

Step 20: Details Page API Integration & Final Polish
-----------------------------------------------------------------------------
- Technologies Used: Mongoose, Next.js Server Components.
- How it was done:
  * `src/app/product/[id]/page.tsx` এ Mongoose ব্যবহার করে ডেটাবেস থেকে ডাইনামিক ডেটা ফেচ করা হয়েছে।
  * পুরো প্রজেক্টটি রিভিউ করে ফাইনাল স্টেপগুলো কমপ্লিট মার্ক করা হয়েছে।

Step 21: Complete Home Page Sections
-----------------------------------------------------------------------------
- Technologies Used: React, Tailwind CSS, Recharts.
- How it was done:
  * `StatisticsSection`, `TestimonialsSection`, এবং `FAQSection` আগে থেকেই তৈরি করা ছিল এবং `page.tsx`-এ সঠিকভাবে রেন্ডার করা হয়েছে।

Step 22: Update Navbar for Authenticated Users
-----------------------------------------------------------------------------
- Technologies Used: NextAuth, React Hooks.
- How it was done:
  * `Navbar.tsx`-এ `useSession` ব্যবহার করে লগিন করা ইউজারদের জন্য 'Dashboard', 'Add Item', 'Manage Items' লিংকগুলো ডায়নামিক ভাবে অ্যাড করা হয়েছে।
  * লগিন করা থাকলে ইউজারের নামের প্রথম অক্ষর দিয়ে একটি অ্যাভাটার এবং একটি Logout বাটন যুক্ত করা হয়েছে।

Step 23: Card Grid Layout & Skeletons
-----------------------------------------------------------------------------
- Technologies Used: Tailwind CSS, Next.js Loading UI.
- How it was done:
  * `ProductGrid.tsx` এর লেআউটটি `lg:grid-cols-3` থেকে পরিবর্তন করে রিকোয়ারমেন্ট অনুযায়ী `lg:grid-cols-4` করা হয়েছে।
  * `ProductSkeleton.tsx` নামে একটি নতুন গ্লোবাল অ্যানিমেটেড স্কেলিটন কম্পোনেন্ট তৈরি করা হয়েছে।
  * `src/app/products/loading.tsx` ফাইল তৈরি করে প্রোডাক্ট ফেচ হওয়ার সময় "Loading..." লেখার বদলে সুন্দর স্কেলিটন রেন্ডার করার ব্যবস্থা করা হয়েছে।

Step 24: Demo Login Button
-----------------------------------------------------------------------------
- Technologies Used: React State.
- How it was done:
  * `LoginForm.tsx`-এ একটি "Use Demo Admin Account" বাটন যুক্ত করা হয়েছে।
  * বাটনে ক্লিক করলে অটোমেটিকভাবে অ্যাডমিন ইমেইল ও পাসওয়ার্ড ফর্মের ইনপুট বক্সে ফিলাপ হয়ে যাবে, যা ইনস্ট্রাক্টরদের টেস্ট করার জন্য সুবিধার হবে।

Step 25: Remove Placeholder Content & Final Polish
-----------------------------------------------------------------------------
- How it was done:
  * সম্পূর্ণ প্রোজেক্ট কোডবেস (About Page, Contact, Product Tabs) চেক করা হয়েছে। কোথাও কোনো "Lorem Ipsum" বা ডামি টেক্সট নেই, সব জায়গায় অর্থপূর্ণ (Realistic) কন্টেন্ট ব্যবহার করা হয়েছে যা রিকোয়ারমেন্টের সাথে সামঞ্জস্যপূর্ণ।
  * পুরো প্রোজেক্টটিকে ১০০% রিকোয়ারমেন্ট অনুযায়ী রেডি করা হয়েছে।
